[workspace]
resolver = "2"
members = ["crates/re0inv", "crates/analyzer", "crates/msgtool"]

[profile.release]
# Debug info in release: no usable debugger inside the game, crash dumps are all we get.
debug = 1
